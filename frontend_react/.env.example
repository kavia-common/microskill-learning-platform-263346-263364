# Frontend Environment Example

# Primary base URL for the backend API. This should include protocol and port.
# Example for local dev where backend runs on 3001:
REACT_APP_API_BASE=http://localhost:3001

# Alternative name supported by some components (fallback logic uses API_BASE first)
REACT_APP_BACKEND_URL=http://localhost:3001

# Optional: Override for assets base if you serve assets from a CDN.
# If not set, assets are resolved relative to REACT_APP_API_BASE (recommended).
# REACT_APP_ASSETS_BASE=http://localhost:3001
