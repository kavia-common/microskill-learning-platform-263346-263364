# PUBLIC_INTERFACE
# Example environment variables for the frontend React app.
# Copy this file to .env and adjust as needed. Do not commit real secrets.

# Backend API base (e.g., http://localhost:3001). If omitted on localhost, defaults to http://localhost:3001; otherwise same-origin is used.
REACT_APP_API_BASE=http://localhost:3001

# Optional: alternative variable recognized by the app (merged with REACT_APP_API_BASE if present)
# REACT_APP_BACKEND_URL=http://localhost:3001

# Optional UI/config flags
REACT_APP_USE_DUMMY=false
REACT_APP_LOG_LEVEL=info

# Optional custom mapping overrides (JSON strings)
# REACT_APP_VIDEO_TITLE_MAP={"Inbox Zero":"quick-inbox-zero"}
# REACT_APP_AUDIO_TITLE_MAP={"Inbox Zero":"quick-inbox-zero"}
