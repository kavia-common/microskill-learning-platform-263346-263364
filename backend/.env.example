# Backend configuration
PORT=3001
NODE_ENV=development
# Allow CORS from frontend
FRONTEND_ORIGIN=http://localhost:3000
# Trust reverse proxy headers if behind proxy
TRUST_PROXY=false
# Logging level: debug | info | warn | error
LOG_LEVEL=info
